<h2>Add Ingredients</h2>
<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, prompt: "Select Ingredient" %>
  <%= f.submit "Save changes" %>
<% end %>